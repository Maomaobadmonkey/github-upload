from math import factorial  # place `import` statement at top of the program


# don't modify this code or variable `x` may not be available
x = int(input())

print(factorial(x))  # use factorial() here